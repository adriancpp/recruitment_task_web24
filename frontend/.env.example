# Opcjonalnie: pełny URL API (np. produkcja). Domyślnie używane jest /api (proxy Vite → Laravel).
# VITE_API_BASE_URL=http://127.0.0.1:8000/api

# Cel proxy w dev (npm run dev) — Laravel `php artisan serve`
VITE_PROXY_TARGET=http://127.0.0.1:8000
